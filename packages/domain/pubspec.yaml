name: domain

environment:
  sdk: ">=2.16.1 <3.0.0"

dependencies:  
  share_plus: ^3.1.0
  geolocator: ^8.2.0
  connectivity_plus: ^2.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.4
  