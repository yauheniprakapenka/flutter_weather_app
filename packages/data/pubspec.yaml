name: data

environment:
  sdk: ">=2.16.1 <3.0.0"

dependencies:
  dio: ^4.0.4
  get: ^4.6.1
  
  flutter_weather_app:
    path: ../../

  domain:
    path: ../domain/  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.4
