/// How many `seconds` the notification will be displayed. Also needed to remove the
/// overlay with notification.
const kDisplayDuration = 2;

/// How many `milliseconds` does the notification appear and disappear.
const kDurationAnimation = 600;
